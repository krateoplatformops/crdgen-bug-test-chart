{
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema",
  "required": [],
  "properties": {
    "enumString": {
      "type": [
        "string"
      ],
      "enum": [
        "1",
        "2"
      ],
      "description": "enumString test where a string is parsed by crd into an integer in the generated CRD"
    },
    "argocd": {
      "type": "object",
      "description": "argocd Test for preserving fields order instead of alphabetical order",
      "required": [],
      "properties": {
        "namespace": {
          "type": [
            "string"
          ],
          "description": "namespace Test for preserving fields order instead of alphabetical order",
          "default": "krateo-system"
        }
      }
    },
    "mlflow": {
      "type": "object",
      "title": "mlflow Test for array not in root of the form",
      "required": [],
      "properties": {
        "metrics": {
          "type": "array",
          "description": "metrics Test for array not in root of the form",
          "items": {
            "type": [
              "string"
            ]
          }
        }
      }
    },
    "metrics": {
      "type": "array",
      "description": "metrics Test for array in root of the form",
      "items": {
        "type": [
          "string"
        ]
      }
    }
  }
}
