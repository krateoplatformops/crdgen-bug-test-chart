{
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema",
  "required": [],
  "properties": {
    "enumString": {
      "type": [
        "string"
      ],
      "enum": [
        "1",
        "2"
      ],
      "title": "enumString test where a string is parsed by crd into an integer in the generated CRD",
      "default": "1"
    },
    "argocd": {
      "type": "object",
      "title": "argocd Test for preserving fields order instead of alphabetical order",
      "required": [],
      "properties": {
        "namespace": {
          "type": [
            "string"
          ],
          "title": "namespace Test for preserving fields order instead of alphabetical order",
          "default": "krateo-system"
        }
      }
    },
    "mlflow": {
      "type": "object",
      "title": "mlflow Test for array not in root of the form",
      "required": [],
      "properties": {
        "metrics": {
          "type": "array",
          "default": [
            ""
          ],
          "title": "metrics Test for array not in root of the form",
          "items": {
            "type": [
              "string"
            ]
          }
        }
      }
    },
    "metrics": {
      "type": "array",
      "default": [
        ""
      ],
      "title": "metrics Test for array in root of the form",
      "items": {
        "type": [
          "string"
        ]
      }
    }
  }
}
